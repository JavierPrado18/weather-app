import React from 'react';

const Spinner = () => {
    return (
        <div>
            <p>Loading weather</p>
            <div className='loader'></div>
        </div>
    );
};

export default Spinner;